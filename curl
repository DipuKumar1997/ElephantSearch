# Click the Variables button, above, to create your own variables.
GET ${exampleVariable1} // _search
{
  "query": {
    "${exampleVariable2}": {} // match_all
  }
}
curl -X PUT "http://localhost:9200/products"
curl http://localhost:9200/products/_count
curl "http://localhost:9200/products/_search"
DELETE /products
GET /_cat/indices/products
GET /_cat/indices/products?v&h=index,docs.count

taking backup using pgdump commands
pg_dump -U postgres -d searchData  -Fc -f backup.dump // filesize goes from 392 in text to ? in postgress to 22 mb in backup.dump
@@ retreving back into postgress
createdb -U postgres searchData // or new database
pg_restore -U postgres -d searchData -v "backup.dump"
check size using ncdu .

sudo apt update
sudo apt install pipx
pipx ensurepath
pipx install git-filter-repo
git-filter-repo --version
pipx ensurepath
~/.local/bin/git-filter-repo --version
git-filter-repo --path PATH/TO/FILE --invert-paths
